import { FondoMarronDirective } from './fondo-marron.directive';

describe('FondoMarronDirective', () => {
  it('should create an instance', () => {
    const directive = new FondoMarronDirective();
    expect(directive).toBeTruthy();
  });
});
